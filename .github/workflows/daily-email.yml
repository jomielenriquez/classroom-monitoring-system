name: Daily Email Trigger

on:
  schedule:
    # Every day at 8 AM Philippines time (UTC+8 → 00:00 UTC)
    - cron: "0 0 * * *"
  workflow_dispatch: # Allows manual trigger

jobs:
  trigger-api:
    runs-on: ubuntu-latest

    steps:
      - name: Call the email sending API
        run: |
          echo "Triggering daily email..."
          curl -X POST "https://tccroommonitoring.somee.com/email/SendSchedule" \
          -H "Content-Type: application/json" \
          -v
